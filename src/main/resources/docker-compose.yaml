services:
  redis:
    image: redis:7-alpine
    container_name: repit-redis
    ports:
      - "6379:6379"
    restart: unless-stopped

  chat:
    build: .
    image: repit-chat
    container_name: repit-chat
    ports:
      - "8080:8080"
    depends_on:
      - redis
    environment:
      SPRING_PROFILES_ACTIVE: prod
      REDIS_HOST: redis
      REDIS_PORT: 6379
      API_SERVER_URL: http://host.docker.internal:8080
      AI_SERVER_URL: http://host.docker.internal:8000
    restart: unless-stopped