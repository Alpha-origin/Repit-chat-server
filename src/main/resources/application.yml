spring:
  application:
    name: repit-chat

  data:
    redis:
      host: localhost
      port: 6379

repit:
  api-server:
    base-url: http://localhost:8080

anthropic:
  api-key: ${ANTHROPIC_API_KEY}
  model: ${ANTHROPIC_MODEL:claude-3-5-haiku-latest}